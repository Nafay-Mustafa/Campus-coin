<x-app-layout><div class="cc-card"><h1 class="cc-title">Saved tips & insights</h1><p class="cc-muted">Keep useful advice here for later.</p>@forelse($bookmarks as $b)<div class="tip"><b>{{ $b->title }}</b><div>{{ $b->content }}</div><form method="POST" action="{{ route('bookmarks.destroy',$b) }}" style="margin-top:8px">@csrf @method('DELETE')<button class="cc-btn danger" style="padding:5px 8px">Remove</button></form></div>@empty<p class="cc-muted">Nothing bookmarked yet.</p>@endforelse</div></x-app-layout>